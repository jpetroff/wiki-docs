# Guidelines

This projects uses `bun` instead of `node` and `npm`. Always use `bun` equivalents.

Always be mindful of usage limits:

* DO NOT run overly extensive end-to-end tests. Cover the bare minimum.
* Search the topic in @/.memory folder by keywords when needed additional information on the project.
* DO NOT run playwright tests unless ordered